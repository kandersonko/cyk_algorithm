class CYKAlgo:
    def __init__(self, G):
        """ initilizes with the grammar G
        """
        self.G = G;

    def membership(w):
        V = generate(w)
        if((check(V)) {
            return true
        }
        return false;


    def generate(w):
        """ generates the array of sets Vii
        """
        pass

    def check(V):
        """ checks that the last element of V contains S (start symbol)
        """
        pass